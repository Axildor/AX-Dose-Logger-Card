## Current Status
**Complete**: Box Override Field Relabel + Drinks Box Rename to Top/Bottom Box (2026-08-08) — completed the labelling cleanup left over from the "Custom Chips → Custom Boxes" rename. The Daily tab Top Box / Bottom Box icon/label/entity override pickers still carried stale product-name labels (`Safe to Take Icon` / `Safe to Take Label` / `Safe to Take Entity`, `Pills Left Icon` / `Pills Left Label` / `Pills Left Entity`), the Drinks tab `In Body Box` / `Disruption Box` headers had the same stale-prefix pattern in both the box headers and their icon/label/entity fields, the Custom Boxes icon/label pickers were label-*suppressed* (so the existing `Box N Icon` / `Box N Label` translation values never reached the user), and the Daily Bottom Box entity-override helper was missing the `Overridden by the Days Left toggle.` note that the Top Box helper already had for its toggle. Architecture plan: [`plans/box-icon-label-override-relabel-plan.md`](plans/box-icon-label-override-relabel-plan.md).

**What Was Changed:**
- **[`src/localize.ts`](src/localize.ts:193)** — Relabelled all four box expandables' entity/icon/label override fields to generic **`Override Entity` / `Override Icon` / `Override Label`** (keys unchanged): Daily Top Box (`config.safe_to_take_entity` / `_label` / `_icon`), Daily Bottom Box (`config.pills_left_entity` / `_label` / `_icon`), Drinks Top Box (`config.in_body_*`), Drinks Bottom Box (`config.disruption_*`). Renamed the Drinks box headers `config.in_body_box` `In Body Box` → `Top Box` and `config.disruption_box` `Disruption Box` → `Bottom Box` for cross-tab positional consistency with the Daily tab. Appended `Overridden by the Days Left toggle.` to `config.helper.pills_left_entity` (mirrors the Top Box `config.helper.safe_to_take_entity` `...Overridden by the Amount in body toggle.` wording). Reworded `config.helper.in_body_icon` `Icon on the In Body box` → `Icon on the box` to match the already-generic `config.helper.disruption_icon` / `config.helper.safe_to_take_icon` wording.
- **[`src/ax-dose-logger-editor.ts`](src/ax-dose-logger-editor.ts:591)** — Two editor expandable `title` literals changed: line 591 `title: 'In Body Box'` → `'Top Box'`; line 640 `title: 'Disruption Box'` → `'Bottom Box'` (per the established pattern, editor `title` is a literal, not a localize key). `computeLabel` (lines ~991-1020): dropped the `_icon` and `_label` clauses from the chip + drink_chip suppression blocks so `chip_N_icon` / `chip_N_label` / `drink_chip_N_icon` / `drink_chip_N_label` fall through to `localize(lang, 'config.' + schema.name)` and render `Box N Icon` / `Box N Label` (the values already existed from the chips→boxes rename but were never reaching the user because the suppression returned `''`). Reworded the stale `// ── Drinks Panel ──` comment block (lines 561-568) `In Body Box` / `Disruption Box` → `Top Box (In Body)` / `Bottom Box (Disruption)` so the bundled dist carries no stale feature references. **Follow-on fix v3 — "Settings" label (user-confirmed, three iterations):** the entity-picker "Chip N" / "Drinks chip N" label could NOT be suppressed via empty string — `computeLabel` `return ''`, inline `label: ''`, AND inline `label: '\u00A0'` (non-breaking space) were ALL humanized by ha-form to the schema `name` (`chip_1` → "Chip 1"). ha-form treats falsy/empty computeLabel returns as "no label" and falls back to humanizing the schema `name`; the inline `label` is ignored when `computeLabel` is provided. Final fix (user suggestion): make `computeLabel` return a **non-empty** label — `localize(lang, 'config.box_settings')` = `"Settings"` — for the 8 entity pickers (`chip_1`..`chip_4` + `drink_chip_1`..`drink_chip_4`). "Settings" is non-empty so it overrides the humanize fallback cleanly, and reads naturally under the "Box N" expandable header (groups the entity picker + the icon/label overrides below it). Added `config.box_settings: 'Settings'` to localize.ts. Removed the redundant inline `label` from all 8 entity-picker schema nodes (computeLabel takes precedence, so the inline label was dead weight). **Custom Box icon+label grid fix (user-reported):** the icon+label grids were structurally identical to the Top Box grid (`type: 'grid'`, `name: ''`, icon + label pickers) but used `column_min_width: '200px'`, which forced a single-column wrap because the Box N expandable is nested 3 levels deep (panel → `chips` expandable → `chip_N_box` expandable → grid), so its available width is narrower than the 2-level-deep Top Box grid. Reduced `column_min_width` to `'180px'` on the 8 Custom Box icon+label grids only (Daily `chip_1`..`chip_4` + Drinks `drink_chip_1`..`drink_chip_4`); the Top/Bottom Box grids (1 level shallower) keep `'200px'` (user-confirmed value). This fix worked — the boxes now render side-by-side.
- **[`README.md`](README.md:83)** — Updated Drinks-tab box-name references for the header rename: visual editor description line 83 (`In Body Box, Disruption Box` → `Top Box, Bottom Box`); Drinks Features line 160 (`The In Body Box and Disruption Box are each fully overridable...` → `The Drinks tab's Top Box and Bottom Box are each fully overridable...`). Config-options table: Drinks rows 218-230 use `Top Box (In Body)` / `Bottom Box (Disruption)` parentheticals + `Override Icon/Label/Entity` wording; Daily rows 202-208 use `Top Box (Safe to Take)` / `Bottom Box (Pills Left)` parentheticals + `Override Icon/Label/Entity` wording (line 212 `pills_left_entity` row now also carries the `Overridden by the Days Left toggle.` note). Left line 155 (rendered-card label description: `In Body` on top, `Disruption` on bottom) as-is — those describe the rendered default labels, not editor headers.
- **[`dist/ax-dose-logger-card.js`](dist/ax-dose-logger-card.js)** — rebuilt via `yarn run build` (clean, exit 0, 4.2s, no warnings). Dist grep confirms `Override Icon` (4), `Override Label` (4), `Override Entity` (4), `Box 1 Icon` (2), `Box 4 Icon` (2), `Overridden by the Days Left toggle` (1) all present; `In Body Box` (0), `Disruption Box` (0), `Safe to Take Icon/Label/Entity` (0), `Pills Left Icon/Label/Entity` (0), `In Body Icon/Label/Entity` (0), `Disruption Icon/Label/Entity` (0) all absent.

**Files Modified:** [`src/localize.ts`](src/localize.ts), [`src/ax-dose-logger-editor.ts`](src/ax-dose-logger-editor.ts), [`README.md`](README.md), [`dist/ax-dose-logger-card.js`](dist/ax-dose-logger-card.js) (rebuilt). No backend / coordinator / store / config-flow / types changes. No `projectstructure.md` change (no files added/renamed/deleted). Architecture plan: [`plans/box-icon-label-override-relabel-plan.md`](plans/box-icon-label-override-relabel-plan.md).

**Key Design Decisions:**
1. **Generic `Override Entity` / `Override Icon` / `Override Label`, not `Top Box Icon` etc.** — the expandable header (`Top Box` / `Bottom Box`) already conveys which box the override applies to; repeating the box name in the field label is redundant. `Override *` is the established HA visual-editor convention and reads naturally. User-confirmed.
2. **Drinks tab boxes → `Top Box` / `Bottom Box`** — the Drinks two-box layout already conceptually uses "Top"/"Bottom" (see [`drinks-panel.ts`](src/components/drinks-panel.ts) comment lines 14-15: `Top "In Body"` / `Bottom "Sleep Disruption"`). The Daily tab already made this trade-off (`Safe to Take Box` → `Top Box`); the inner toggle/select/label fields carry the semantic meaning. Cross-tab positional naming consistency is the user's stated goal.
3. **Custom Boxes: un-suppress, don't rename to `Override Icon`** — the `Box N Icon` / `Box N Label` values already existed from the chips→boxes rename but were never reaching the user because `computeLabel` returned `''`. Un-suppressing is a one-line-per-block editor change with zero localize edit, and the rendered labels (`Box 1 Icon` etc.) match the expandable header (`Box 1`) for clear per-box identity — no need for generic `Override Icon` here because the Custom Boxes don't have a product-name legacy to erase and the `Box N` prefix gives the per-box context the Top/Bottom boxes get from their expandable headers. User-confirmed.
4. **Entity-picker stays suppressed in Custom Boxes** — the `Box N (optional)` external label is redundant inside its own `Box N` expandable. Only the icon/label override fields un-suppress.
5. **Entity-picker renamed to `Override Entity`** (user-confirmed) — all four entity-picker fields became `Override Entity` for full consistency with the `Override Icon` / `Override Label` siblings. The default-sensor semantic is retained via the helper text (which still names the default sensor per box) and the README config-table parentheticals (`(Safe to Take)` / `(Pills Left)` / `(In Body)` / `(Disruption)`).
6. **`Overridden by the Days Left toggle.` mirrors the Top Box helper** — `config.helper.safe_to_take_entity` ends `...Overridden by the Amount in body toggle.`; the Bottom Box has the same relationship (Days-Left toggle overrides any custom `pills_left_entity`), so the parallel wording is the consistency fix. The Drinks `disruption_entity` helper already had its parallel note (`...Overridden by the Time to Low selector.`) — no change.
7. **README uses `Top Box (In Body)` / `Bottom Box (Disruption)` parentheticals** — the editor headers change to positional names, but the config keys (`in_body_*` / `disruption_*`) and the rendered default labels ("In Body" / "Disruption") are unchanged. The parenthetical retains the semantic mapping so a user cross-referencing the README config table with the editor or the rendered card isn't confused.
8. **Values-only rename, keys untouched** — exactly mirrors the ["Panel/pane → Tab/tab"](plans/unify-category-naming-plan.md) and "Custom Chips → Custom Boxes" precedents. Internal config keys are persistence identifiers invisible to end users; changing them would force a config migration with no user benefit.
9. **No backend change / no migration / no `projectstructure.md` change** — the feature is entirely frontend (editor labels + helper text + README). The backend has no "icon" / "label override" concept; those are card-config-only fields. No files added/renamed/deleted.

**Verification:** `yarn run build` clean (`dist/ax-dose-logger-card.js` created in 4.2s, exit 0, no warnings). Dist grep confirms the new strings present and all stale strings absent (counts above). No backend verification needed (frontend-only change).

### Previous Context — Custom "Chips" → "Boxes" Naming Convention Rename (2026-08-08)
**Previous Current Status:**
**Complete**: Custom "Chips" → "Boxes" Naming Convention Rename (2026-08-08) — renamed the user-facing **"Custom Chips"** feature labels to **"Custom Boxes"** in the card's visual editor (Daily tab + Drinks tab settings) so the editor matches what actually renders. The `chip_1`..`chip_4` / `drink_chip_1`..`drink_chip_4` custom-chip entities render as box-style tiles, not small pill-style chips, so the old "Chip N" / "Custom Chips" labels contradicted the rendered output. **Both tabs renamed together** (user-confirmed). Frontend strings-only rename: only translation *values* changed in localize.ts; *keys* unchanged → no lookup regressions; only the two editor expandable `title` literals changed → existing saved configs keep working with **zero migration**. Internal config keys, CSS class names, and TS identifiers deliberately untouched. Architecture plan: [`plans/custom-chips-to-boxes-rename-plan.md`](plans/custom-chips-to-boxes-rename-plan.md). Full detail + 8 key design decisions archived in [`memory-bank/old/activeContext-archive.md`](memory-bank/old/activeContext-archive.md:1).

### Previous Context — Daily Tab Top Box — Amount in Body Toggle + Submenu Rename (2026-08-08)
**Previous Current Status:**
**Complete**: Daily Tab Top Box — Amount in Body Toggle + Submenu Rename (2026-08-08) — added a toggle to the Daily tab's top stat-pill box that switches its displayed entity from the default Safe to Take sensor to the Amount in Body sensor, and renamed the two Daily Tab editor submenus from "Safe to Take Box" / "Pills Left Box" to **"Top Box"** / **"Bottom Box"** so the submenu names stay truthful regardless of which sensor the box renders. **Safe to Take stays the default** (toggle OFF) — preserves all existing configs with zero migration. Full detail archived in [`memory-bank/old/activeContext-archive.md`](memory-bank/old/activeContext-archive.md:1).

### Previous Context — Daily/Drinks Button Two-Line Height Lock (2026-08-08)
**Previous Current Status:**
**Complete**: Daily/Drinks Button Two-Line Height Lock (2026-08-08) — fixed the Daily pane "Take Pill" button growing ~19px taller when its "Last: … • Next: …" sub-text wraps to two lines. Added `min-height` to `.take-sub` (later refined to `min-height: 8em` on the button itself) in both panels and `flex: 1` to `.stat-pill` so the two boxes split the column's taller height evenly. Full detail archived in [`memory-bank/old/activeContext-archive.md`](memory-bank/old/activeContext-archive.md:1).

---

> 📜 **Older history archived in [`memory-bank/old/activeContext-archive.md`](memory-bank/old/activeContext-archive.md:1)** — read it only if the truncated file above lacks context for the current task. The archive holds the full pre-truncation content (current status + prior-status blocks + earlier "Previous Context" sections back to the project's earliest work).
