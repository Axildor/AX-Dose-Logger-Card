## Current Status
**Complete**: Sync Editor Toggle Visual Defaults with Runtime Defaults (2026-08-08) — fixed 4 of 17 boolean toggles in the visual editor that rendered visually OFF while being functionally ON by default. The `confirm_tool_actions` toggle (reported by user) plus three graph-related toggles (`show_amount_in_body`, `show_day_avg_boxes`, `show_adherence_boxes`) all use a `!== false` negative-false runtime test (treating `undefined` as ON), but their editor schema entries declared `{ selector: { boolean: {} } }` with no `default`, so HA's `ha-form` rendered `undefined` as OFF. The fix adds `default: true` to all 4 out-of-sync boolean selectors; `ha-form` honors a schema entry's `default` when the data value is `undefined`, so the toggles now render ON for existing cards (where the field is undefined) **without** baking the value into persisted config — preserving decision #18 (no baked defaults). The other 13 toggles were audited and confirmed correctly in sync (they use `=== true`/truthy tests where `undefined` is genuinely OFF, or `!== true` where the runtime default is genuinely OFF like `hide_nav_bar`; the 8 chip `show_icon` toggles are explicitly "Off by default" per their helper text). Also updated the comment block above `settings_panel` to reflect the "Settings Tab" rename + the new `default: true` rationale.

**What Was Changed:**
- **[`src/ax-dose-logger-editor.ts`](src/ax-dose-logger-editor.ts)** — Added `default: true` to 4 boolean selectors: `show_amount_in_body` (line ~912), `show_day_avg_boxes` (line ~936), `show_adherence_boxes` (line ~940), `confirm_tool_actions` (line ~969). Updated the comment block above the `settings_panel` expandable ("Settings Panel"→"Settings Tab", "Tools panel"→"Tools tab", added the `default: true` rationale referencing decision #18).
- **[`dist/ax-dose-logger-card.js`](dist/ax-dose-logger-card.js)** — rebuilt via `yarn run build`, clean (exit 0, 3.7s); dist verified to contain all 4 `default: true` entries (lines 1726, 1751, 1756, 1788) + the updated comment block.

**Files Modified:** [`src/ax-dose-logger-editor.ts`](src/ax-dose-logger-editor.ts), [`dist/ax-dose-logger-card.js`](dist/ax-dose-logger-card.js) (rebuilt). No runtime / `setConfig` / `getStubConfig` / localize / types / README changes. No `projectstructure.md` change (no files added/renamed/deleted).

**Key Design Decisions:**
1. **`default: true` on the schema entry, not in `setConfig`** — `ha-form` honors the schema `default` only for visual rendering when data is `undefined`; it does NOT write the default into the persisted config. This preserves decision #18 (no baked defaults in `setConfig`) while fixing the visual mismatch.
2. **Only the 4 out-of-sync toggles fixed** — the other 13 were audited and confirmed correct; adding `default: true` to them would create a *new* mismatch (visually ON but functionally OFF).
3. **Negative-false runtime tests stay** — `!== false` is the correct pattern for default-ON fields; the bug was purely on the editor-rendering side, not the runtime side.
4. **`getStubConfig` left as-is** — it sets `show_amount_in_body: true` for freshly-added cards (covers the new-card case), but does nothing for existing cards where the field is `undefined` in saved YAML. The schema `default` covers both cases uniformly, so the stub value is redundant but harmless; left as-is to avoid touching runtime config.

**Verification:** `yarn run build` clean (`dist/ax-dose-logger-card.js` created in 3.7s, exit 0, no warnings). Dist grep confirms all 4 `default: true` entries present + the 13 in-sync toggles unchanged (e.g. `stats_3_columns` at line 1769 has no `default`).

### Previous Context — Unify Card Settings Category Naming "Panel"→"Tab" (2026-08-08)
**Previous Current Status:**
**Complete**: Unify Card Settings Category Naming — "Panel"/"pane" → "Tab"/"tab" (2026-08-08) — consumer-facing string unification across the frontend card. The card had a 3-way terminology split: the editor section headers said "X Panel" (Daily Panel, Drinks Panel, Graphs Panel, Stats Panel, Settings), the helper/description text said "pane" ("Show as a chip on the Daily pane."), and the on-card nav bar labels were bare ("Daily", "Graphs", etc.). Converged the editor + helper text on "tab"/"Tab" while leaving the visible nav-bar labels bare (unchanged). "Tab" was chosen over "panel" because the nav bar is functionally a tab bar (a row of icon buttons switching the card's visible content), consumers know "tab" from browsers/app tab bars, and "panel" mildly collides with Home Assistant's own sidebar-panel vocabulary (Lovelace, Energy, Settings).

**What Was Changed:**
- **[`src/localize.ts`](src/localize.ts)** — Editor section labels: `config.daily_panel` 'Daily Panel'→'Daily Tab', `config.graphs_panel` 'Graphs Panel'→'Graphs Tab', `config.stats_panel` 'Stats Panel'→'Stats Tab', `config.drinks_panel` 'Drinks Panel'→'Drinks Tab', `config.settings_panel` 'Settings'→'Settings Tab'. Helper text: `config.helper.confirm_tool_actions` '...Tools panel action...'→'...Tools tab action...', `config.helper.chip` '...Daily pane.'→'...Daily tab.', `config.helper.drink_chip` + `config.helper.drink_chips` '...Drinks pane.'→'...Drinks tab.', `config.helper.show_amount_in_body` '...Graphs pane.'→'...Graphs tab.', `config.helper.hide_nav_bar` '...pane navigation bar.'→'...tab navigation bar.'.
- **[`README.md`](README.md)** — Full pass replacing consumer-facing "pane"→"tab" and "Panel"→"Tab" across the Visual Editor description, Features section, Medicine/Drinks card section headings ("Medicine Panes"→"Medicine Tabs"), all section prose (Daily/Graphs/Stats/Drinks/Inventory/Tools), and the Configuration Options reference table (~30 occurrences). Internal code-identifier references in prose were left as the bare section names ("Daily", "Graphs") matching the on-card nav labels.
- **[`dist/ax-dose-logger-card.js`](dist/ax-dose-logger-card.js)** — rebuilt via `yarn run build`, clean (exit 0, 3.9s); dist verified to contain the new "Daily Tab"/"Drinks Tab"/etc. strings.

**Files Modified:** [`src/localize.ts`](src/localize.ts), [`README.md`](README.md), [`dist/ax-dose-logger-card.js`](dist/ax-dose-logger-card.js) (rebuilt). No backend / coordinator / store / config-flow / editor / types changes. No `projectstructure.md` change (no files added/renamed/deleted).

**Key Design Decisions:**
1. **"Tab" over "Panel" for editor categories + helper text** — matches what the user clicks (a tab bar of icon buttons), avoids HA's sidebar-panel terminology collision, and unifies the 3 previously-inconsistent surfaces.
2. **On-card nav-bar labels stay bare** ("Daily", "Graphs", etc.) — no "Tab" suffix, so the visible card UI is unchanged; only the editor + helper text gained the "tab" wording.
3. **`Settings` also gets the "Tab" suffix in the editor** ("Settings Tab") for category consistency, even though it is not a nav-bar tab — it holds the `confirm_tool_actions` toggle. The nav bar keeps its current icon-only Tools behavior (Settings is not on the nav bar).
4. **Internal code identifiers untouched** — `_activePane`, `pane-btn`, `pane.*` keys, `*-panel.ts` component files, and custom-element tags (`ax-dose-daily-panel`) stay as-is; invisible to consumers, and renaming would be a broad refactor with no user benefit. Only the *displayed values* of the translation strings changed; the translation *keys* (`config.daily_panel`, etc.) are unchanged so no lookup regressions.
5. **Config schema `name` fields untouched** — `daily_panel`, `drinks_panel`, etc. in `ax-dose-logger-editor.ts` stay as-is, so existing saved configs keep working with no migration.

**Verification:** `yarn run build` clean (`dist/ax-dose-logger-card.js` created in 3.9s, exit 0, no warnings). Dist file grep confirms the new "Daily Tab"/"Drinks Tab"/"Graphs Tab"/"Stats Tab"/"Settings Tab"/"Tools tab action"/"Daily tab."/"Drinks tab."/"Graphs tab."/"tab navigation bar" strings are present.

### Previous Context — Tools Panel Dose Tools Heading + Color-Scheme Fix + Confirm-Action Toggle (2026-08-08)
**Previous Current Status:**
**Complete**: Tools Panel — Dose Tools Heading + Color-Scheme Fix + Confirm-Action Toggle (2026-08-08) — three coupled frontend changes to the Tools pane (Pane 4) of [`ax-dose-tools-panel`](src/components/tools-panel.ts). **(1) New "Dose Tools" section:** removed Skip Dose from "Adherence Tools" and created a new "Dose Tools" heading containing Skip Dose + Undo Dose, placed between Adherence Tools (Reset Adherence %, Mark Last Adherence Taken) and General Tools (Reset History). Added a `tools.dose_header` localize key ("Dose Tools") + a new `hasDoseTools` gate; updated the file-header comment to document the 3-section structure. **(2) Color-scheme fix:** removed the hardcoded red `danger` class from Reset Adherence %, Reset History, and Undo Dose so they render as plain `.tool-btn` using `--rgb-primary-color` (the card's configured color scheme via [`getColorOverrides`](src/helpers.ts:78) at the `ha-card` level), matching Skip Dose / Mark Last Adherence Taken. The `.tool-btn.danger` CSS block stays for the per-granular-drink master Undo/Reset buttons (still red — semantically correct for a destructive per-drink reset). **(3) Confirm-action toggle:** added a new collapsible "Settings" expandable to the visual editor holding a `confirm_tool_actions` boolean (default ON). A new `runToolAction(title, descriptor, onConfirm)` controller method centralizes the dialog-vs-direct decision in the container — when ON it opens the existing [`_renderToolsDialog`](src/ax-dose-logger-card.ts:1758) warning popup first; when OFF it fires `onConfirm` immediately with no popup. All 7 Tools panel handlers (adherence reset/cover/skip, reset history, undo, drink undo/reset) now call `controller.runToolAction(...)` instead of `controller.openToolsDialog(...)`. `openToolsDialog` is kept for backward compatibility.

**Previous What Was Changed:**
- **[`src/types.ts`](src/types.ts)** — Added `confirm_tool_actions?: boolean` to `AxDoseLoggerCardConfig` (default-on doc, negative-false check) + `runToolAction(...)` to `CardController` interface (kept `openToolsDialog`).
- **[`src/ax-dose-logger-card.ts`](src/ax-dose-logger-card.ts)** — Added `_runToolAction` (negative-false check → dialog vs direct fire) + public `runToolAction` accessor next to `openToolsDialog`.
- **[`src/components/tools-panel.ts`](src/components/tools-panel.ts)** — Switched all 7 handlers from `openToolsDialog` → `runToolAction`; restructured `render()` into 3 sections (Adherence / Dose / General) with `hasDoseTools` gate; removed `danger` class from Reset Adherence %, Reset History, Undo Dose; updated file-header comment.
- **[`src/ax-dose-logger-editor.ts`](src/ax-dose-logger-editor.ts)** — Added `settings_panel` expandable after `stats_panel` with `confirm_tool_actions` boolean selector; updated computeHelper expandable-name comment.
- **[`src/localize.ts`](src/localize.ts)** — Added `tools.dose_header` ("Dose Tools"), `config.settings_panel` ("Settings"), `config.confirm_tool_actions` ("Confirm Tool Actions"), `config.helper.confirm_tool_actions`.
- **[`README.md`](README.md)** — Restructured Tools section into 3 grouped bullets + confirm-toggle note; added `confirm_tool_actions` row to config options table.
- **[`dist/ax-dose-logger-card.js`](dist/ax-dose-logger-card.js)** — rebuilt via `yarn run build`, clean (exit 0, 4.1s).

**Previous Files Modified:** [`src/types.ts`](src/types.ts), [`src/ax-dose-logger-card.ts`](src/ax-dose-logger-card.ts), [`src/components/tools-panel.ts`](src/components/tools-panel.ts), [`src/ax-dose-logger-editor.ts`](src/ax-dose-logger-editor.ts), [`src/localize.ts`](src/localize.ts), [`README.md`](README.md), [`dist/ax-dose-logger-card.js`](dist/ax-dose-logger-card.js) (rebuilt). No backend / coordinator / store / config-flow changes (frontend-only). No `projectstructure.md` change (no files added/renamed/deleted).

**Previous Key Design Decisions:**
1. **`runToolAction` centralizes the dialog-vs-direct decision in the container** — the Tools panel stays purely presentational; the controller checks `config?.confirm_tool_actions !== false` so existing configs (without the field) keep the always-confirm behavior with no migration.
2. **Section ordering Adherence → Dose → General** — Dose Tools groups the two dose-level actions (skip + undo) between the adherence actions and the whole-history reset (most destructive, stays last).
3. **Color fix via removing the `danger` class** — the plain `.tool-btn` class already uses `--rgb-primary-color` (overridden at the `ha-card` level via `getColorOverrides()`), so removing the class is the minimal fix with no new CSS. The `.tool-btn.danger` block stays for the per-granular-drink master Undo/Reset buttons.
4. **Default-on via `!== false`** — optional field, negative-false test preserves existing behavior; no config migration needed.
5. **`openToolsDialog` kept for backward compatibility** — the public accessor + interface method remain; the new `runToolAction` is the preferred entry point for Tools panel actions.

**Previous Verification:** `yarn run build` clean (`dist/ax-dose-logger-card.js` created in 4.1s, exit 0, no warnings).


### Previous Context — Inventory Panel Label "Name Unit Left" (2026-07-11)

**Previous Current Status:**
**Complete**: Inventory Panel Label — "Name Unit Left" (2026-07-11) — reworked the Master Tracker Inventory panel's col-1 first-line label from `Name left` (e.g. "Coffee left") to `Name Unit Left` (e.g. "Tea Bags Left") per user request. The drink's configured `unit_of_measurement` (e.g. "Bags", "Cups", "Cans") is now interpolated between the drink name and the "Left" suffix, and the "Left" suffix was capitalized. The unit is read live from the stock entity's `unit_of_measurement` state attribute via [`c.getAttr(d.stockEntityId, 'unit_of_measurement')`](src/components/inventory-panel.ts:65) at render time, so it stays in sync if the user re-configures the unit via the options flow — no `DrinkInfo` type change needed. Falls back gracefully to `Name Left` when the unit attribute is absent or non-string (mirrors the `daily-panel.ts` / `drinks-panel.ts` unit-display pattern). The 2nd-line "Est. days left" + value is unchanged.

**Previous What Was Changed:**
- **[`src/components/inventory-panel.ts`](src/components/inventory-panel.ts)** — `_renderRow`: added 3 lines after the `stockState` read — `stockUnit`, `unitSegment`, `leftLabel`. The template's `<span class="stat-label">` now renders `${leftLabel}`.
- **[`src/localize.ts`](src/localize.ts)** — `inventory.left` value: `'left'` → `'Left'`.
- **[`README.md`](README.md)** — Inventory section col-1 description updated.
- **[`dist/ax-dose-logger-card.js`](dist/ax-dose-logger-card.js)** — rebuilt (exit 0, 1.9s).

**Previous Key Design Decisions:**
1. Read the unit live from the stock entity (not `DrinkInfo`) — stays in sync with options-flow reconfiguration.
2. Graceful fallback to `Name Left` when the unit attribute is absent/non-string.
3. Capitalize "Left" — title-cased label matches the medicine "Pills Left" convention.
4. No `DrinkInfo` type change / no backend change — localized to the panel + one localize value.

**Previous Verification:** `yarn run build` clean (exit 0, no warnings).


### Prior status — archived on 2026-07-11 (Sleep Disruption Popup improvements)

**Previous Current Status:**
**Complete**: Master Stats Box Removal + Inventory 2-Line "Est. days left" (2026-07-11) — four paired frontend changes following the backend removal of `DrinkMasterDaysLeftSensor`. **(1) Master Stats "Total Doses" box removed:** deleted the `totalDoses` mapping for master devices in [`_computeEntities`](src/ax-dose-logger-card.ts:363) (the `dose_count + pk_model` block) — the master has no dedicated total-doses sensor; the body-mass sensor still maps to `amountInBody` (the "Amount in Body" row stays). Medicine devices keep their suffix-based `_total_doses` mapping. **(2) Master Stats "Est. days left" row removed:** deleted the `masterRole === 'days_left'` mapping in the master branch of [`_computeEntities`](src/ax-dose-logger-card.ts:355) — the backend no longer creates the master days-left sensor (removed in v14). The Stats panel's `if (e.daysLeft)` guard now skips the row for master devices. The granular-drink branch is preserved so granular devices still resolve `daysLeft` (classifier completeness). **(3) Inventory col-1 2-line layout:** restructured the Inventory panel's col-1 `.stat-pill` from a single line (icon + name + stock) to a 2-line layout inside a new `.stat-text` wrapper: line 1 = drink name + stock value; line 2 = "Est. days left" label + the per-drink `DrinkDaysLeftSensor` value (plain number, no unit suffix). The icon stays in its **exact current position** (left, after padding + gap) — `align-items: center` on `.stat-pill` keeps it vertically centered against the full 2-line text block height; `.stat-text` takes `flex: 1` so the text fills the space to the right of the icon. Box size + text sizes unchanged (`.stat-label` 15px, `.stat-value` 18px, new `.stat-sublabel` 13px + `.stat-subvalue` 16px mirror the col-2 `.avg-label`/`.avg-value`). **(4) `daysLeftEntityId` on `DrinkInfo`:** added the field to the [`DrinkInfo`](src/types.ts:229) interface, resolved it in [`_getDrinksOfSubstance`](src/ax-dose-logger-card.ts:753) (sensor branch: `role === ...

**What Was Changed:**
- **[`src/types.ts`](src/types.ts)** — Added `daysLeftEntityId?: string` to `DrinkInfo` (with JSDoc).
- **[`src/ax-dose-logger-card.ts`](src/ax-dose-logger-card.ts)** — (1) `_computeEntities` master branch: removed the `masterRole === 'days_left'` mapping + the `dose_count + pk_model` totalDoses mapping (replaced with an explanatory comment). (2) `_getDrinksOfSubstance` sensor branch: added `else if (role === 'days_left') info.daysLeftEntityId = entityId;`. (3) `_relevantStateChanged` inventory watch list: added `if (d.daysLeftEntityId) watchedIds.push(d.daysLeftEntityId);`.
- **[`src/components/inventory-panel.ts`](src/components/inventory-panel.ts)** — `_renderRow`: added days-left state read + `daysLeftDisplay` computation (plain number via `formatInteger`, no unit suffix). Col-1 HTML restructured: icon + new `.stat-text` wrapper containing two `.stat-line` rows (name+stock / Est. days left+value, reusing `stats.days_left_est` localize key). CSS: added `.stat-text` (flex column, gap 4px, flex:1), `.stat-line` (space-between baseline row), `.stat-sublabel` (13px secondary), `.stat-subvalue` (16px weight-600). `.stat-value` lost `margin-left: auto` (space-between handles alignment now). `.stat-pill` unchanged (icon stays centered).
- **[`README.md`](README.md)** — Inventory section: updated col-1 description to mention the 2-line layout + "Est. days left" 2nd line. Stats (Drinks) section: removed "Total doses aggregated across every granular drink" + "Days left (est. days left — inventory burn rate aggregated across all granular drinks)" rows; added "Last drink timestamp" row.
- **[`dist/ax-dose-logger-card.js`](dist/ax-dose-logger-card.js)** — rebuilt (exit 0, 3.3s).

**Files Modified:** [`src/types.ts`](src/types.ts), [`src/ax-dose-logger-card.ts`](src/ax-dose-logger-card.ts), [`src/components/inventory-panel.ts`](src/components/inventory-panel.ts), [`README.md`](README.md), [`dist/ax-dose-logger-card.js`](dist/ax-dose-logger-card.js) (rebuilt). No backend / coordinator / store / config-flow / localize / editor changes. No `projectstructure.md` change (no files added/renamed/deleted — only edits to existing files).

**Key Design Decisions:**
1. **Icon position preserved exactly** — the icon stays at the left (after `padding: 12px 14px` + `gap: 8px`), exactly where it was. `align-items: center` on `.stat-pill` keeps it vertically centered against the full 2-line text block height. `.stat-text` takes `flex: 1` so the text fills the remaining space to the right of the icon. The icon does NOT move to the center of the box.
2. **Plain number, no unit suffix** — the Inventory 2nd line shows the days-left value as a plain number (e.g. `12`), not `12 d` or `12 days`. The "Est. days left" label already conveys the unit.
3. **Reuse `stats.days_left_est` localize key** — no new translation strings; the Inventory 2nd line label reuses the existing Stats panel key ("Est. days left").
4. **Master `totalDoses` removal is master-only** — medicine devices still map `totalDoses` via the `_total_doses` suffix; only the master branch's `dose_count + pk_model` mapping is deleted.
5. **Granular-drink `daysLeft` mapping preserved** — granular devices still resolve `daysLeft` in `_computeEntities` (classifier completeness), even though they redirect to the master. The new `daysLeftEntityId` on `DrinkInfo` is the per-drink variant used by the Inventory panel.
6. **`_relevantStateChanged` watch list** — adding `daysLeftEntityId` ensures a days-left state change (e.g. after a dose moves the 7-day average) triggers an inventory re-render without waiting for the 30s tick.

**Verification:** `yarn run build` clean (`dist/ax-dose-logger-card.js` created in 3.3s, exit 0, no warnings). Paired backend `hass -c ./config --script check_config` exit 0 (see backend repo's memory bank). No `projectstructure.md` change.



**Follow-on refinement (2026-07-11, same day):** three Inventory panel adjustments per user feedback. **(a) Font-size parity:** the 2nd-line "Est. days left" label + value now use the SAME sizes as the 1st-line device name + stock (label 15px, value 18px) — previously the 2nd line was smaller (13px/16px). **(b) "left" suffix:** the device-name line now reads "device-name left" (e.g. "Coffee left") — added a new `inventory.left` localize key ("left") appended to the drink name. **(c) Stats-page formatting:** the Inventory grid + boxes now mirror the Stats pane visual language — 2-col grid with 8px gap, boxes with `padding: 10px 8px`, `border-radius: 10px`, `background: rgba(primary, 0.05)`, `transition: background 0.15s ease`, 16px icon at opacity 0.7 inside a new `.stat-pill-header` row (icon + 2-line `.stat-text` block). The icon stays in its exact current position (left, vertically centered via `align-items: center` on the header row). `.stat-pill` changed from a row flex (icon direct child) to a column flex with a `.stat-pill-header` wrapper holding the icon + text, matching the Stats `.stat-cell` internal structure. `.avg-cell` (col-2) adopted the same padding/radius/background for parity. **Files Modified:** [`src/localize.ts`](src/localize.ts) (new `inventory.left` key); [`src/components/inventory-panel.ts`](src/components/inventory-panel.ts) (HTML: `.stat-pill-header` wrapper + "left" suffix; CSS: full rewrite to Stats visual language + 2nd-line font sizes 15px/18px); [`README.md`](README.md) (Inventory section updated); [`dist/ax-dose-logger-card.js`](dist/ax-dose-logger-card.js) (rebuilt, exit 0, 3s). `yarn run build` clean.



**Second follow-on refinement (2026-07-11):** three more Inventory panel CSS adjustments. **(a) Day Average font parity:** `.avg-label` 13px → 15px and `.avg-value` 16px → 18px so the col-2 Day Average box text matches the col-1 inventory box text sizes (both 15px label / 18px value). **(b) Line spacing parity:** confirmed `.stat-text` gap (4px between the two `.stat-line` rows) and `.avg-cell` gap (4px between the two `.avg-line` rows) are already identical — the perceived difference was due to the prior font-size mismatch (now fixed by a). **(c) Icon 50% bigger:** `.stat-pill-header ha-icon` `--mdc-icon-size` 16px → 24px (50% increase). **Files Modified:** [`src/components/inventory-panel.ts`](src/components/inventory-panel.ts) (3 CSS value changes); [`dist/ax-dose-logger-card.js`](dist/ax-dose-logger-card.js) (rebuilt, exit 0, 3s). `yarn run build` clean.



**Third follow-on refinement (2026-07-11):** decreased the inter-line gap in both Inventory boxes from 4px to 3px — `.stat-text` gap (col-1, between the two `.stat-line` rows) and `.avg-cell` gap (col-2, between the two `.avg-line` rows) both changed 4px → 3px per user request for tighter line spacing. **Files Modified:** [`src/components/inventory-panel.ts`](src/components/inventory-panel.ts) (2 CSS gap values); [`dist/ax-dose-logger-card.js`](dist/ax-dose-logger-card.js) (rebuilt, exit 0, 2.8s). `yarn run build` clean.



**Fourth follow-on refinement (2026-07-11):** the 4px→3px gap change was imperceptible because the default `line-height` (~1.2) adds ~3-4px above + below each text line, dwarfing the 1px gap delta. Fix: decreased the inter-line gap further to **2px** AND added `line-height: 1.1` to both `.stat-text` (col-1) and `.avg-cell` (col-2) so the text blocks themselves are tighter. The combination of 2px gap + 1.1 line-height produces a noticeably tighter inter-line spacing. **Files Modified:** [`src/components/inventory-panel.ts`](src/components/inventory-panel.ts) (gap 3px→2px on .stat-text + .avg-cell; added line-height: 1.1 to both); [`dist/ax-dose-logger-card.js`](dist/ax-dose-logger-card.js) (rebuilt, exit 0, 3.1s). `yarn run build` clean.

---

> 📜 **Older history archived in [`memory-bank/old/activeContext-archive.md`](memory-bank/old/activeContext-archive.md:1)** — read it only if the truncated file above lacks context for the current task. The archive holds the full pre-truncation content (current status + 10 prior-status blocks + earlier "Previous Context" sections back to the project's earliest work).
